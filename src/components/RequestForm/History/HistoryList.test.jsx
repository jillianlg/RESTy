import React from 'react';

const HistoryItem = ({}) => {
  return (
    <article id={id}>
      <p id={id}>{method}</p>
      <span id={id}>{url}</span>
    </article>
  )
}

export default HistoryItem;
